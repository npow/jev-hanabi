# Exploratory run (incomplete)

Stopped after 2 games when a decision audit showed that written Choice instructions alone did not enforce safe-play priority. The enforced action protocol was evaluated separately in `results_hle_signal_protocol_100/`.

Raw games are preserved for diagnosis; do not report this as a full benchmark. `partial_manifest.json` records the completed seeds and raw-data hash.
